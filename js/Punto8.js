// 8) numero divisible en dos
let NumeroDiv = parseInt(prompt("Indique su numero y le dire si es divisible en 2"));
let Divisor = 2

if (NumeroDiv % Divisor === 0){
    document.write("Si es divisible campeon")
} else{
    document.write("El numero iungresado no es divisible en 2")
}
